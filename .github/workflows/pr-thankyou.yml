on:
  pull_request:
    types:
      - opened

jobs:
  testing-action:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Post PR Comment
        uses: sureshgithubrit/Docker-Action-pr-gihy-comment@main
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }} #by default it is having read access
          giphy-api-key: ${{ secrets.GIPHY_API_KEY }}
          
      - name: Post javascript action PR Comment
        uses: sureshgithubrit/js-action-pr-ghipy-comment@1.0.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }} #by default it is having read access
          giphy-api-key: ${{ secrets.GIPHY_API_KEY }}
